import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class GemsService {

  constructor() { }

  // Methods for calculating the numbers for each property from historical data.

  // Store data
  //     - Local?
  //     - IndexDB?

  // Methods for retrieving and writing the data
}
